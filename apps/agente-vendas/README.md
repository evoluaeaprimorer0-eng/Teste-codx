# Agente de Vendas (projeto separado)

Este projeto foi criado de forma independente em `apps/agente-vendas`, sem alterar os arquivos da raiz.

## Como executar

```bash
cd apps/agente-vendas
npm start
```

A aplicação sobe em `http://localhost:3001` por padrão.
